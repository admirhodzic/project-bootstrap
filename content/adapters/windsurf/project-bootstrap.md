# Project Bootstrap

Follow the root `AGENTS.md`; it is the canonical policy. Load a relevant workflow from `.agents/skills/` when needed. Do not infer authority for external or destructive actions, and preserve unrelated work.
